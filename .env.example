# Copy this to ~/.claude/.env (or export in your shell) and fill in your key.
# Get a Grok key at https://x.ai/api
# Keep the real .env private:  chmod 600 ~/.claude/.env
XAI_API_KEY=xai-your-key-here
