# Agent Context

Project instructions live in **`CLAUDE.md`** in this directory. Read it first — the content is
tool-agnostic despite the filename, and this repo ships a **Codex adapter** as a first-class
target alongside the Claude Code one (`adapters/`).

Shared personal context (global conventions, memory index, cached research) is mapped in
`~/.codex/AGENTS.md`.
